rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    function isAdmin() {
      return request.auth.token.role == 'admin'
    }

    match /certificates/{userId}/{allPaths=**} {
      allow create, update, delete, get, list :request.auth != null
    }
  }
}
