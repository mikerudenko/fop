import { makeStyles } from '@material-ui/styles';

export const useInvoiceFormStyles = makeStyles({
  backToLink: {
    marginBottom: '20px',
    textAlign: 'right',
    marginLeft: 'auto',
  },
  topBar: {
    display: 'flex',
  },
  formWrapper: {
    paddingBottom: 50,
  },
});
