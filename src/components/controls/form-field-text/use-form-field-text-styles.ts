import { makeStyles } from '@material-ui/styles';

export const useFormFieldTextStyles = makeStyles({
  formControl: {
    width: '100%',
    marginBottom: 10,
  },
  input: {
    width: '100%',
    marginBottom: 0,
  },
});
