import { makeStyles } from '@material-ui/styles';

export const useFormAutocompleteStyles = makeStyles({
  formControl: {
    width: '100%',
    marginBottom: 10,
  },
  input: {
    width: '100%',
    marginBottom: 0,
  },
});
