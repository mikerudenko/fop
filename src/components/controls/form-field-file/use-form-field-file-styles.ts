import { makeStyles } from '@material-ui/styles';

export const useFormFieldFileStyles = makeStyles({
  button: {
    marginBottom: 20,
  },
  input: {},
  label: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
  },
});
