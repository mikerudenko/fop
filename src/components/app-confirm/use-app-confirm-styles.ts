import { makeStyles, Theme } from '@material-ui/core/styles';

export const useAppConfirmStyles = makeStyles((theme: Theme) => ({
  buttons: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
  },
}));
